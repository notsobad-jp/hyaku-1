<h1>Listing histories</h1>

<p><%= link_to 'Try New Songs', new_history_path %> | <%= link_to 'Review Old Songs', new_review_path %> | <%= link_to 'Try Exam', new_exam_path %></p>

<table>
  <tr>
    <th>User</th>
    <th>Song</th>
    <th>Times</th>
    <th>Result</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<% @histories.each do |history| %>
  <tr>
    <td><%= history.user_id %></td>
    <td><%= history.song_id %></td>
    <td><%= history.created_at %></td>
    <td><%= link_to 'Show', history %></td>
    <td><%= link_to 'Edit', edit_history_path(history) %></td>
    <td><%= link_to 'Destroy', history, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
