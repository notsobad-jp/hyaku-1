%div(id="next_song" class="songs")
  = render "menu_bar"
  %section.main
    %h1.menu_title
      採点結果
    #finish
      %p.score.serif
        <em>#{@score}</em>/100
      %p
        おつかれさまでした。<br>
      = link_to( "ホームへ戻る", :root, :class => "btn" )

