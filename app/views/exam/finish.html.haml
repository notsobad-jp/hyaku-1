%div(id="next_song" class="songs")
  %section.main
    = image_tag("menu_exam.jpg", :id => "menu_img")
    %h1.menu_title
      採点結果
    #finish
      %p.score.serif
        <em>#{@score}</em>/100
      %p
        おつかれさまでした。<br>
      = link_to( "ホームへ戻る", :root, :class => "btn-none" )

