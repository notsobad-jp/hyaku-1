- user = current_user || User.find(0)
%div.sub-box
  %div.profile.cf
    = profile_image
    %div
      %p#name #{user.email}
      - if current_user
        %small
          = link_to '(ログアウト)', logout_path
      - else
        %small.warning
          ※ログインしていない状態だと学習の記録が残りません。
- if current_user
  %div.sub-box
    %div.degree
      %p.serif #{degree(best_score)}
  %div.sub-box
    %ul.status
      %li
        学習済み：<em>#{last_song_id}</em>/100歌
      %li
        100首まで：残り<em>#{days_left(last_song_id)}</em>日
      %li
        ベストスコア：<em>#{best_score}</em>/100
- else
  %div.sub-box
    = link_to image_tag("fb_login.png", :class => "fb_login", :id => "head_login"), auth_at_provider_path(:provider => :facebook)
    %p
      %small (登録時に勝手につぶやいたりしません)
%div.sub-box
  %script(type="text/javascript")
    <!--
    google_ad_client = "ca-pub-7703471297541925";
    /* 百/一 */
    google_ad_slot = "7095539561";
    google_ad_width = 200;
    google_ad_height = 200;
    //-->
  %script(type="text/javascript" src="http://pagead2.googlesyndication.com/pagead/show_ads.js")
